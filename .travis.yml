language: generic 
sudo: required
services:
    - docker

before_install:
    - docker build -t cryofracture/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true cryofracture/docker-react npm run test